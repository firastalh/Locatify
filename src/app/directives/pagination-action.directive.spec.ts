import { PaginationActionDirective } from './pagination-action.directive';

describe('PaginationActionDirective', () => {
  it('should create an instance', () => {
    const directive = new PaginationActionDirective();
    expect(directive).toBeTruthy();
  });
});
